import { CtrlClickDirective } from './ctrl-click.directive';

describe('CtrlClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlClickDirective();
    expect(directive).toBeTruthy();
  });
});

import { DarkColorDirective } from './dark-color.directive';

describe('DarkColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkColorDirective();
    expect(directive).toBeTruthy();
  });
});

import { InputClickSelectDirective } from './input-click-select.directive';

describe('InputClickSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new InputClickSelectDirective();
    expect(directive).toBeTruthy();
  });
});
